spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/lucaflix
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  jwt-secret: dev-secret-key
  jwt-expiration-milliseconds: 86400000
  base-url: http://localhost:8080

cors:
  allowed-origins: "*"

admin:
  email: admin@lucaflix.com
  password: admin123
  firstname: Admin
  lastname: Local
  username: admin